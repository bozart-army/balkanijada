generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Models will be added here later based on /docs/04_DATA_MODEL_OVERVIEW.md
